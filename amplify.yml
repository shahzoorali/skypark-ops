version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p dist
        - cp index.html app.js seed.js styles.css dist/
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
